<section class="min-w-0 space-y-6">
  <%- include("../../partials/product-management-tabs", { activeProductManagementTab: "categories" }) %>
  <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
    <div>
      <h1 class="text-3xl font-black text-slate-900">Danh mục</h1>
      <p class="mt-1 text-slate-500">Quản lý phân loại sản phẩm cho thuê.</p>
    </div>
    <% if (currentUser.role === "admin") { %>
      <a href="/categories/new" class="inline-flex rounded-full bg-brand-500 px-5 py-3 font-semibold text-white transition hover:bg-brand-700">Thêm danh mục</a>
    <% } %>
  </div>

  <div class="min-w-0 overflow-hidden rounded-3xl bg-white shadow-lg shadow-slate-200/60">
    <div class="table-scroll">
      <table class="min-w-full divide-y divide-slate-200">
      <thead class="bg-slate-50">
        <tr>
          <th class="px-6 py-4 text-left text-xs font-bold uppercase tracking-[0.25em] text-slate-500">Mã</th>
          <th class="px-6 py-4 text-left text-xs font-bold uppercase tracking-[0.25em] text-slate-500">Mô tả</th>
          <th class="px-6 py-4 text-right text-xs font-bold uppercase tracking-[0.25em] text-slate-500">Thao tác</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-100">
        <% categories.forEach((category) => { %>
          <tr>
            <td class="px-6 py-4 font-semibold text-slate-900"><%= category.code %></td>
            <td class="px-6 py-4 text-slate-600"><%= category.description %></td>
            <td class="px-6 py-4">
              <div class="flex justify-end gap-2">
                <% if (currentUser.role === "admin") { %>
                  <a href="/categories/<%= category._id %>/edit" class="rounded-full bg-amber-100 px-4 py-2 text-sm font-semibold text-amber-800">Sửa</a>
                  <form action="/categories/<%= category._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Xóa danh mục này?');">
                    <button type="submit" class="rounded-full bg-rose-100 px-4 py-2 text-sm font-semibold text-rose-800">Xóa</button>
                  </form>
                <% } %>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
      </table>
    </div>
  </div>
</section>
